
projectedSecret:
  - secret:
      name: "properties-2021-06-07"

image:
  tag: unstable-reg

env:
  APP_ENV: reg

ingress:
  enabled: true
  hosts:
    - host: "lti-tool-manager-reg.apps-test.iu.edu"
      paths: []

activeProfiles: "redis,reg"

serviceAccountName: lti-tool-manager-reg

cluster: es-test